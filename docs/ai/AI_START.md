# AI Start — Retired Index

The repository's single startup index is
[AI_START_HERE.md](../../AI_START_HERE.md).

This file remains only as a compatibility pointer for existing links. It does
not define a separate startup order and cannot override the root index.

Use `docs/reference/` for detailed packet, gameplay, enemy, loot, and client-DLL reports. Use `docs/archive/` for historical AI handoffs and old session notes.
