# Current Task

NPC / player locomotion is being replaced with client `Vehicle_t` mechanics.
Path follow + `EnsureSurfaceAlignment` tripod probe are in. Still open:
`GraphPathFinder_t` instead of Recast.
