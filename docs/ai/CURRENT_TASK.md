# Current Task

Active work: Mike approved the production cutover of exact application SOURCE_SHA `bb5dd9165c6201b15ef1e0cfbfd788a3e205486a` and only the four required inventory/mission migrations. Preserve all production data, verify the backup by restoration, validate all existing rows before NewEngine starts, and restore the database before any rollback to Legacy.

Deployment tooling repair: staged executable validation and a frozen, pinned prior-pair entry point for a separately approved schema migration. Windows shell validation and 64 Linux deployment regression cases PASS. Actual production startup checks PASS; NewEngine read-only readiness confirms the four pending migrations. Isolated restore, migration of all 280 inventory rows, unchanged schema/data fingerprints for all 40 original tables, and restoration back to that exact baseline PASS. Production backup refresh, migration and service switch remain to be performed. Evidence: ignored `build-verify/live-bb5dd916/`. Runtime artifacts remain the previously accepted bb5dd916 binaries; tooling changes do not relabel their acceptance. Official-client CharInPlay and clean-restart gameplay checks remain unverified for this exact build.
