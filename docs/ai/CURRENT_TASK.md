# Current Task

Production cutover is complete at application SOURCE_SHA `bb5dd9165c6201b15ef1e0cfbfd788a3e205486a`. Four explicitly approved inventory/mission migrations passed; all 280 inventory rows and all 40 original tables were preserved. LoginEngine and ZoneEngine_New are live with zero restarts; ChatEngine and account broker are healthy. Verified backup and database-restore evidence are retained. See `docs/reports/BB5DD916_PRODUCTION_CUTOVER_RECEIPT.md`.

Active repair: live character-selection handoff succeeds, but world entry rejects Legacy sparse stat rows and base-only nano maxima. The three failed production snapshots are regression fixtures. Restore only the four evidenced missing defaults at DAO hydration, compute vital ability trickle and the existing GM health override, and enforce effective vital bounds after equipment/active-nano hydration before publication. No stored character/inventory rows or schema are being rewritten. Focused fixture checks pass; full Windows testing in the primary checkout has only the 11 already-known missing Playfields input failures. Exact Windows/Linux acceptance will run in the existing fully supplied controlled checkout before deploying the repair. After NewEngine writes, Legacy rollback requires the validated database restore/reconciliation path; changing executables alone is unsafe.
