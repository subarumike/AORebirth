# Current Task

## Active

Close the four remaining runtime-consumer gap classes from validated
`44fa42fce316d98c1d5f6326d92caae909398289` on
`codex/zoneengine-final-runtime-consumers` in the existing isolated reconciliation
worktree. The Delmus 77-file reconciliation is complete and frozen. Current
origin/master and merge base are `6e90dda030774726aa2060acb9edb756ea1f635c`.
Reconstruct the 489 ordinary bindings; implement accepted per-capability consumers,
27 remaining dialogue routes, N02/N05/N07/N08/N10 and explicit N03 compatibility.
No guessed fallbacks, generic NPC nano AI, production changes or client use.
Master promotion requires all accepted contracts and same-SHA Windows/Linux gates.

### Current implementation and outstanding work

The source ledger now reconstructs all 489 bindings, but no ordinary actor
consumer is claimed connected. N05 Mongo 100198/100194 is implemented with atomic
initial healing/cost and explicit combat NPC eligibility. Mike approved N03:
preserve the saved appearance and allow login. All four recognized morphs restore
over that opaque baseline; cancellation/expiry remove only their owned effect.
New-cast requirements remain unchanged. The NewEngine suite passes 479/479.

Precommit Windows passes all 12 mandatory stages, 1129/1129 AOtomation tests,
Release and disposable migration/rollback/restart. Commit/push this candidate,
then validate exact committed Windows and Linux at the identical SHA. Do not
reuse historical checkpoint receipts.
Concrete implementation blockers remain: 322 Subway plus 167 Temple ordinary
consumers, 27 dialogue routes and N02/N07/N08/N10. They are not missing data merely
because their New consumers are absent. The exact contracts and checkpoint
boundaries are in `docs/evidence/ZONEENGINE_NEW_RUNTIME_CONSUMER_CHECKPOINT.md`.

Preserve the approved schema and existing transaction/ownership boundaries.
Only disposable database validation is authorized. No production operation,
occupied-sidecar conversion, live client use, generic NPC nano AI or fallback.
Master remains unchanged until every supported regression is resolved and the
same final SHA passes both Windows and Linux. Completed Delmus work stays in
`docs/evidence/DELMUS_D2D98446_RUNTIME_RECONCILIATION.md`, not a new review here.
