---
description: ZoneEngine_New work follows repository governance and current evidence
globs: AORebirth/Server/ZoneEngine_New/**
alwaysApply: false
---

# ZoneEngine_New Work

Follow root `AGENTS.md` and the `AI_START_HERE.md` instruction chain.
`ZoneEngine_New` is active; Legacy `ZoneEngine` is historical evidence, not
current ownership. Inspect the existing implementation and configuration and
prove they are insufficient before proposing architecture changes. Use capture
evidence under the applicable current workflow and evidence rules. This Cursor
rule does not define a preferred replacement architecture.
