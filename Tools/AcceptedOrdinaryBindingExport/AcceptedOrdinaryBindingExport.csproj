<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- Offline only: evaluate the existing catalog, never start the Legacy engine.
         This cross-platform project compiles the authoritative shared source. -->
    <ProjectReference Include="../../LinuxBuild/Projects/ZoneEngine.Linux.csproj" />
  </ItemGroup>
</Project>
